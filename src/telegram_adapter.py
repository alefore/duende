# DO NOT EDIT. This file is automatically generated by Duende.
import asyncio
import logging
import pathlib
from telegram import Update
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes

from message_bus import MessageBus, Message as BusMessage, MessageId, SenderName, SessionId
from swarm_config import load_config, SwarmConfig

logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO)


class Handler:

  def __init__(self, config: SwarmConfig, message_bus: MessageBus) -> None:
    assert config.telegram
    self._chat_id: int | None = None
    self._config = config
    self._message_bus = message_bus

  async def start(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    assert update.message
    await update.message.reply_text(
        "I'm awake! Send me any message and I'll repeat it.")

  async def echo(self, update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Receives a message from Telegram.

    The message is written to the bus, with the `config.telegram.consumer` value
    as the recipient.

    A response is sent to the chat and logged: f"Message received ({id=})"
    """
    assert update.effective_chat
    self._chat_id = update.effective_chat.id
    # ✨ write message to bus and respond
    assert update.effective_chat
    assert update.message
    self._chat_id = update.effective_chat.id

    assert self._config.telegram  # Ensure telegram config is not None
    sender = self._config.telegram.end_user_identity
    recipient = self._config.telegram.consumer_agent
    session_id = None
    body = update.message.text or ""

    bus_message = BusMessage(
        id=MessageId(0),  # ID will be overwritten by the message bus
        sender=sender,
        recipient=recipient,
        session_id=session_id,
        body=body,
    )

    message_id = await self._message_bus.write_new_message(bus_message)

    response_text = f"Message received (id={message_id})"
    await update.message.reply_text(response_text)
    logging.info(
        "Telegram message from chat_id=%s sent to bus with id=%s",
        self._chat_id,
        message_id,
    )
    # ✨

  async def _read_message_bus(self):
    """Calls wait_for_new_messages and dispatches messages to the user.

    If self._chat_id is not set, polls doing nothing until it gets set.

    When it receives messages, calls self._app.bot.send_message to dispatch
    them to self._chat_id.

    Calls `mark_message_as_seen` on all messages processed.

    Only receives messages with recipient set to
    `config.telegram.end_user_identity.`
    """
    # ✨ read message bus
    while True:
      if self._chat_id is None:
        logging.info("Telegram chat_id not set yet, waiting...")
        await asyncio.sleep(1)
        continue

      assert self._config.telegram # Ensure telegram config is not None
      recipient = self._config.telegram.end_user_identity

      # Wait for new messages for the specific recipient
      messages = await self._message_bus.wait_for_new_messages(recipients=[recipient])

      for message in messages:
        logging.info(f"Dispatching message {message.id} to chat_id={self._chat_id}: {message.body}")
        try:
          # Send message to the user via Telegram bot
          await self._app.bot.send_message(chat_id=self._chat_id, text=message.body)
          # Mark message as seen after successful dispatch
          await self._message_bus.mark_message_as_seen(message.id)
          logging.info(f"Message {message.id} marked as seen.")
        except Exception as e:
          logging.error(f"Failed to dispatch message {message.id} to chat_id={self._chat_id}: {e}")
          # Optionally, handle retries or different status for failed dispatches.
          # For now, just log the error and continue.
    # ✨

  async def run(self) -> None:
    await self._message_bus.open()
    assert self._config.telegram
    assert self._config.telegram.token
    self._app = Application.builder().token(self._config.telegram.token).build()
    self._app.add_handler(
        CommandHandler(
            "start",
            self.start,
            filters=filters.User(self._config.telegram.authorized_users)))
    self._app.add_handler(
        MessageHandler(
            filters.TEXT & ~filters.COMMAND
            & filters.User(self._config.telegram.authorized_users), self.echo))

    asyncio.create_task(self._read_message_bus())

    async with self._app:
      await self._app.initialize()
      await self._app.start()
      assert self._app.updater
      await self._app.updater.start_polling()
      print("Bot is polling... Press Ctrl+C to stop.")
      while True:
        await asyncio.sleep(1)


async def main() -> None:
  config = await load_config(pathlib.Path('swarm/config.json'))
  assert config.telegram
  await Handler(config, MessageBus(config.message_bus_path)).run()


if __name__ == '__main__':
  asyncio.run(main())